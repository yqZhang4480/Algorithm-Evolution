#解答/ 

``` C++
class Solution {
public:
	double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
		size_t k = nums1.size() + nums2.size();
		size_t start1 = 0;
		size_t start2 = 0;
		size_t end1 = nums1.size();
		size_t end2 = nums2.size();
		size_t all = nums1.size() + nums2.size();
		size_t k = ((all % 2 == 0) ? 1 : 0) + all / 2;
		getMinK() 
    }
	size_t getMinK(vector<int>& nums1, size_t start1, size_t end1, vector<int>& nums2, size_t start2, size_t end2, size_t k) {
		
	}
};
```