#2025/02/24 #解答/ #解答/已解出/ #解答/评价/时间/

```cpp
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        deque<size_t> q;
        vector<int> ans(nums.size() + 1 - k, 0);
		int maxx = -114514;
        for (size_t i = 0; i < k; ++i) {
	        maxx = max(maxx, nums[i]);
	        
	        q.emplace_front(i);
	        
        }
		ans[0] = maxx;
		for (size_t i = 1; i + k < nums.size(); ++i) {
			
		}
    }
};
```

结果