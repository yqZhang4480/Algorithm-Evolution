#2024/06/10 #解答/ #解答/已解出/ #解答/评价/时间/ #解答/评价/空间/ 

``` cpp
class Solution {
public:
	bool searchMatrix(vector<vector<int>>& matrix, int target) {
		size_t n = matrix.size();
		size_t i_min = 0, j_min = 0,
			   i_max = n, j_max = n;
		while (i_min <= i_max && j_min <= j_max) {
			size_t i = (i_max + i_min) / 2, j = (j_max + j_min) / 2;
			if (i_min == i && j_min == j) { return matrix[i][j] == target; }
			if (auto r = matrix[i][j]; r == target) { return true; }
			else if (r < target) { i_min = i; j_min = j; }
			else { i_max = i; j_max = j; }
		}
		return false;
	}
};
```

结果