#2025/02/24 #解答/ #解答/已解出/ #解答/评价/时间/

``` cpp
class MyQueue {
public:
	stack<int> s[2];

    MyQueue() {}
    void push(int x) {
        s[0].emplace(std::move(x));
    }
    int pop() {
		arrange();
		if (!s[1].empty()) { ret. }
    }
    
    int peek() {
        
    }
    
    bool empty() {
        
    }

	void arrange() {
		while (!s[0].empty()) {
			s[1]
		}
	}
};
```

结果